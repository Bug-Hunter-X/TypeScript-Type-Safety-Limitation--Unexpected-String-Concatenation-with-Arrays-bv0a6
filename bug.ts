function greeter(person: string): string {
  return "Hello, "+ person + "!";
}

let user = ["Jane", "Doe"];

console.log(greeter(user)); //This will compile and run, but may not produce the expected result